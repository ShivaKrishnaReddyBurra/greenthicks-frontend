/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: 'https://greenthicks.live',  // change this to your real website URL
  generateRobotsTxt: true,
  sitemapSize: 7000,
};

export default config;
